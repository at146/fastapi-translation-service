ENVIRONMENT=
# UVICORN
UVICORN_HOST=
UVICORN_PORT=
UVICORN_RELOAD=
# GPU server: set to 1 (multiple workers compete for the same CUDA device)
# CPU server: can be >1 if RAM fits N copies of the model
# Check: uv run python -c "import torch; print(torch.cuda.is_available())"
UVICORN_WORKERS=

MODEL_PATH=

API_KEY=

DOCKER_IMAGE_BACKEND=
TAG=
